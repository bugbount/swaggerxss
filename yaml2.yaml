swagger: '2.0'
securityDefinitions:
  oauth2PasswordSecurity:
    type: oauth2
    flow: password
    tokenUrl: 'https://examples.com/V2/user/{user_id}/oauth/token'
info:
  version: "0.0.1"
  title: Example Title
  description: Please Authorize! <img src=x onerror=alert(1) />
paths:
  /:
    get:
      responses:
        200:
          description: Successful respons
